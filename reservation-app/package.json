{
  "name": "reservation-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Pact consumer for creating reservations",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js --runInBand"
  },
  "dependencies": {
    "@pact-foundation/pact": "16.4.0"
  },
  "devDependencies": {
    "jest": "^30.0.0"
  }
}
